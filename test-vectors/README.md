# Test Vectors

This directory is reserved for shared QuixiCore test vectors.

Test vectors should be organized by contract area:

- `quant/` for quant format fixtures
- `kernels/` for kernel input/output fixtures

Large generated artifacts should be reproducible from documented scripts in backend repositories or external artifact storage. The umbrella repository should keep the canonical fixture definitions and small reference fixtures.

