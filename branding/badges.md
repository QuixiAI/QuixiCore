# Badges

Suggested badge categories for backend repositories:

- Contract version compatibility
- Backend status
- Supported architecture family
- Correctness suite status
- Benchmark suite status

The umbrella repository should avoid badges that imply implementation coverage unless they point to contract or documentation status.

