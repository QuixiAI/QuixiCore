# Optimizer Kernels

This specification covers optimizer kernels used by QuixiCore backends.

## Contract Notes

Optimizer specifications should define update equations, parameter grouping, supported precision modes, optional fused behavior, and correctness tolerances against a reference implementation.

