# Mamba And SSD Kernels

This specification covers Mamba and structured state-space decode and prefill kernels.

## Contract Notes

The contract should define state layout, scan semantics, chunking behavior, supported sequence lengths, supported data types, and numerical tolerance expectations.

