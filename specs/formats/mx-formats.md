# MX Formats

This specification tracks MX quantization formats for QuixiCore.

The contract should define scale sharing, element packing, rounding behavior, supported arithmetic paths, and accumulation expectations.

